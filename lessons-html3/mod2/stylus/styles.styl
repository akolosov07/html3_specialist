vendorize(property, value)
	-webkit-{property} value
	-moz-{property} value
	-o-{property} value
	{property} value

abc(color)
	color color
	border 1px solid color
	
body
	background #333
	color red
	
.myblock
	vendorize(border-radius, 10px)
	abc(red)